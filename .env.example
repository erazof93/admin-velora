# URL base del backend NestJS de Velora (incluye el prefijo de versión).
# En Vercel: configúrala en Project Settings → Environment Variables.
VITE_API_URL=https://backend-running-production.up.railway.app/api/v1

# Nombre visible de la app (opcional).
VITE_APP_NAME=Velora Admin
